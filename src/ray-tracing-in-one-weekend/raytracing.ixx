// Holds most of the code of the book, with some changes:
// * constexpr has been applied where it makes sense.
// * Trailing return type syntax has been used for functions.
// * PascalCase instead of snake_case.
export module raytracing;
export import :math;
export import :vec3;
export import :color;
export import :ray;

